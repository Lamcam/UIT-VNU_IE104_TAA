const auth = require('./auth.model')
const product = require('./product.model')
const unAuth = require('./unauth.model')
const account = require('./account.model')

module.exports = {
    auth,
    product,
    unAuth,
    account,
}